[project]
name = "knowledge-intake"
version = "0.2.0"
description = "Inventory-first source intake for clean, structured Markdown knowledge packages"
requires-python = ">=3.11,<3.14"
dependencies = [
  "crawl4ai==0.9.2",
  "pypdf>=5.0,<7",
]

[tool.uv]
package = false
